apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  volumeName: mysql-pv  # ← دي اللي بتربطهم
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

